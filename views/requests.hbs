<section class="main-section" id="requests-list">
  <div class="search-top-buttons">
    <a class="searchbox-button band">Accepted</a>
    <a class="searchbox-button stage">Rejected</a>
  </div>
  <div class="request-container">
    <div class="request-accepted">
      {{#each requests}}
        {{#if (statusFilter this.status)}}
          {{> requestCard}}
        {{/if}}
      {{/each}}
    </div>
    <div class="request-rejected">
      {{#each requests}}
        {{#unless (statusFilter this.status)}}
          {{> requestCard}}
        {{/unless}}
      {{/each}}
    </div>
  </div>
</section>

<script src='../javascripts/requestButtons.js'></script>
